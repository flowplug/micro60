1. Set live to y, validate to n, train to n in crypfig.ini
2. nu_loop.py reads in a list of targets in control/conflate_fulln_targs.txt
either modify this or pass a list to the function that calls strundow.py that
has the target or targets in it, e.g. ['NVDA'].
2.1 Run create_fill_file.py
3.Make sure the mensell and menbuy fields in crypfig point to the right paths.
4. Set write_adabost to y in crypfig.ini
